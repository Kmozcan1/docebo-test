<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DoceboTest" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/charcoalBlue</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="searchViewStyle">@style/SearchViewStyle</item>
        <item name="android:navigationBarColor">@color/charcoalBlue</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <item name="toolbarStyle">@style/ToolBarStyle</item>

        <item name="circularProgressIndicatorStyle">@style/CircularProgressStyle</item>

        <item name="android:textViewStyle">@style/TextViewStyle</item>

        <item name="tabStyle">@style/TabLayoutStyle</item>
    </style>

    <!--Cold launch theme to display logo-->
    <style name="Theme.DoceboTest.Launcher">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/logo_drawable</item>
    </style>


</resources>