<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="repositoriesFragment" type="com.kmozcan1.docebotest.presentation.ui.RepositoriesFragment"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/constraintLayout4"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".presentation.ui.RepositoriesFragment">

        <Button
            android:id="@+id/sortRepositoriesButton"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:onClick="@{repositoriesFragment::onSortButtonClick}"
            app:icon="@drawable/ic_baseline_sort_24"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/sortDirectionButton"
            style="@style/Widget.AppTheme.Button.IconButton"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            app:icon="@drawable/ic_sort_ascending"
            app:layout_constraintBottom_toBottomOf="@+id/sortRepositoriesButton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/sortRepositoriesButton"
            app:layout_constraintVertical_bias="0.0" />

        <com.kmozcan1.docebotest.presentation.ui.PaginatedListView
            android:id="@+id/repositoriesListView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/sortRepositoriesButton" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
